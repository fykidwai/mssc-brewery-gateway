logging:
  level:
    org:
      springframework:
        cloud:
          gateway: debug
server:
  port: 8080
spring:
  application:
    name: sfg-brewery-gateway
  zipkin:
    base-url: http://localhost:9411
